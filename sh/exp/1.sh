#!/bin/bash
# GPU 0 - insert_HDMI
source "$(dirname "${BASH_SOURCE[0]}")/_common.sh"
$EVAL insert_hole vision_only 0
# $EVAL put_bottle_in_shelf univtac     0
# $EVAL put_bottle_in_shelf vision_only 0