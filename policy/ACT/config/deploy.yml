# Basic experiment configuration
policy_name: ACT
seed: 0
ckpt_setting: 50
instruction_type: seen
instuction_file: null
# ckpt_dir: leave empty; resolved at runtime via CKPT_ROOT env var
#   CKPT_ROOT/<task_name>/<task_config>/policy_last.ckpt
ckpt_dir: null
